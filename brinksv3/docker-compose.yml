version: '3.8'

services:
  people-counter:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./yolov8n.pt:/app/yolov8n.pt
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
    container_name: people-counter-api
